# תכנית תיקון — מאגר מצב הלקוח (client state store)

**מקור:** `QA-FINDINGS-MANIFEST.md` (מקור אמת) + `QA-REPORT-whatsapp-sessions.md`, `QA-REPORT-whatsapp-topics.md`, `QA-REPORT-whatsapp-writing.md`
**קבוצה:** כל הממצאים שמקורם ב"מאגר המצב" של מסך עדכוני הוואטסאפ
**תאריך:** 26.07.2026 · **סטטוס:** תכנית בלבד — שום קוד לא שונה

---

## מצב בסיס — לפני כל שינוי

| מה נבדק | תוצאה |
|---|---|
| חבילת בדיקות אוטומטיות | **לא קיימת.** אין `test` ב-`admin/package.json`, אין קובץ בדיקה אחד בפרויקט (מחוץ ל-`node_modules`) |
| בדיקת טיפוסים | **לא קיימת.** אין TypeScript, אין `tsc`, אין ESLint מוגדר |
| בדיקת תחביר | הרצתי `node --check` על כל 18 קבצי המודול של המסך — **18/18 עוברים** |
| כפילויות קוד | אין. יש עותק אחד בלבד של המאגר, אין עותק שרת מקביל, אין service worker שמחזיק גרסה ישנה |

**המשמעות:** אין שום רשת ביטחון אוטומטית. כל תיקון כאן מאומת בעיניים בלבד. זה מגדיל את חשיבות סעיף "נעילה מפני באגים עתידיים" בכל תיקון.

### ⚠️ אילוץ שמשפיע על האימות עצמו

לפי המניפסט, בסיס הנתונים המקומי והפרודקשן הם **אותו בסיס נתונים**. כלומר: כל ניסיון לאמת תיקון על ידי יצירת סשן חדש והרצת מחקר — **נוגע בנתוני פרודקשן ומוציא כסף אמיתי מחשבון Anthropic**.

לכן בכל תיקון כתבתי, במפורש, **איך לאמת בלי להוציא כסף** — כמעט כל הבדיקות כאן אפשריות על סשן קיים, בלי להריץ מחקר ובלי לייצר טיוטה.

---

## סיכום

| # | התקלה | מזהים | גודל התיקון | סיכון | דורש החלטה? |
|---|---|---|---|---|---|
| 1 | הפוסט המוכן ורשימת המקורות נמחקים מהמסך | `A4#1` ≡ `A1#3` 🔴 · `A1#6` 🟡 | בינוני — 2 קבצים | בינוני | ❓ כן (סעיף החלטה א') |
| 2 | שאילתה שנוספה ידנית נמחקת | `A2#9` 🟠 | קטן — 2 קבצים | נמוך-בינוני | ❓ כן (סעיף החלטה ב') |
| 3 | נושא מסומן והתמקדויות נגררים מסשן קודם | `A2#6` 🟠 | קטן — קובץ אחד | נמוך | לא |
| 4 | חלון "צור טיוטה" נפתח מעל טיוטה קיימת | `A1#7` ≡ `A4#8` 🟡 | קטן — קובץ אחד | נמוך | לא |
| — | תיבת הצ'אט שלא עושה כלום | `X2` 🔴 | — | — | ❓ כן (סעיף החלטה ג') |

**שורש משותף:** שלושה מארבעת התיקונים נובעים מאותה בעיה אחת. הסבר מלא בפרק "השורש המשותף" מיד למטה — כדאי לקרוא אותו לפני התיקונים עצמם.

---

## השורש המשותף — למה כל אלה קורים

יש במסך פונקציה מרכזית אחת שתפקידה "לקבל את הסשן מהשרת ולעדכן את המסך". היא נקראת מ-**14 מקומות שונים** בקוד.

הבעיה: ל-14 המקומות האלה יש **שתי כוונות שונות לגמרי**, והפונקציה מכירה רק אחת מהן.

| הכוונה | מי עושה את זה | מה נכון שיקרה |
|---|---|---|
| **"עבור לסשן אחר"** — 4 מקומות (סשן חדש, פתיחה מהיסטוריה, שכפול, כניסה מקישור) | פותח משהו אחר | לאפס הכול. ✅ הפונקציה עושה בדיוק את זה |
| **"רענן את הסשן הנוכחי"** — 10 מקומות (חזרה לטאב, שליחת הודעה בצ'אט, אישור מחקר, שמירת עריכה, בחירת נושא, סקר תקופתי, הגדרות כתיבה, שמירה סופית ועוד) | מבקש מידע עדכני על **אותו** סשן | לעדכן את מה שהשרת יודע, ולא לגעת במה שקיים רק במסך |

הפונקציה מתייחסת לשני המקרים כאילו הם אחד — ומאפסת הכול, תמיד. לכן בכל אחת מ-10 הפעולות היומיומיות ברשימה השנייה, שלושה דברים שקיימים רק במסך נמחקים: **הפוסט המוכן**, **רשימת המקורות**, ו**שאילתה שהוספת ידנית**.

זו הסיבה שהתיקון המרכזי הוא במקום אחד, ומכסה מספר ממצאים בבת אחת.

---

## סדר ביצוע מומלץ

1. **תיקון 1** ראשון — הוא נוגע בלב המאגר, ותיקון 2 בונה עליו.
2. **תיקון 2** מיד אחריו — הוא מוסיף שורה אחת לאותו בלוק שתיקון 1 יוצר. **שני התיקונים נוגעים באותה פונקציה — אל תריץ אותם במקביל בשני סשנים.**
3. **תיקון 3** — עצמאי לחלוטין, קובץ אחר. אפשר בכל שלב.
4. **תיקון 4** — עצמאי, קובץ אחר. אבל **כדאי לבצע אותו אחרי תיקון 1**, כי בלי תיקון 1 קשה בכלל להבחין אם התסמין נעלם.

אין תלות בשרת. כל הארבעה הם צד-לקוח בלבד — **אין צורך בפריסה מוקדמת של שום דבר בשרת, ואין שינוי בנתונים השמורים**.

---

## תיקון 1 — הפוסט המוכן ורשימת המקורות נמחקים מהמסך

**מזהים:** `A4#1` ≡ `A1#3` (🔴 חוסם) · `A1#6` (🟡 מטריד)

### מה שבור היום

אתה בשלב הכתיבה, הטיוטה על המסך. אתה עובר לטאב אחר לרגע וחוזר — או פשוט כותב משהו בשורת ההודעות למטה. הפוסט **נעלם**. איתו נעלמים מונה המילים, כפתור ההעתקה, שש הפעולות המהירות וכפתור "שמור כסופי". במקביל, פאנל המקורות בצד מתרוקן ומציג "אין ממצאים עדיין".

הטקסט לא אבד — הוא שמור. אבל שום דבר במסך לא אומר את זה, ואין דרך להחזיר אותו חוץ מרענון מלא של הדף.

### התיקון

ללמד את הפונקציה המרכזית להבדיל בין שני המקרים: אם הסשן שנטען הוא **אותו סשן** שכבר פתוח — לשמור על הפוסט ועל רשימת המקורות שכבר במסך, ולעדכן רק את מה שבאמת הגיע מהשרת. אם זה **סשן אחר** — לאפס הכול, בדיוק כמו היום.

### גודל

שני מקומות בקוד: הפונקציה המרכזית עצמה, ושינוי סדר קטן בקוד שמצייר את הצ'אט (הסבר למה הוא הכרחי — בסעיף "מה זה עלול לשבור").

### מה עוד מגיע לקוד הזה

מיפיתי את כל 14 המקומות שקוראים לפונקציה. לכל אחד: האם התיקון משנה אותו.

**ארבעה מקומות שהם "מעבר לסשן אחר" — לא מושפעים כלל:**

| נקודת כניסה | למה לא מושפעת |
|---|---|
| יצירת סשן חדש (חלון "סשן חדש") | מזהה שונה → איפוס מלא, בדיוק כמו היום |
| פתיחת סשן מרשימת "סשנים קודמים" | מזהה שונה → איפוס מלא |
| שכפול סשן | הסשן המשוכפל הוא מזהה חדש → איפוס מלא. **חשוב:** בלי הבדיקה הזו, השכפול היה יורש את המקורות של הסשן המקורי — באג חדש שנמנע |
| כניסה מקישור ישיר (`?session=…`) | טעינה ראשונה, אין סשן קודם → איפוס מלא |

**עשרה מקומות שהם "רענון של אותו סשן" — כולם מושפעים, וזו בדיוק המטרה:**

| נקודת כניסה | מה משתנה |
|---|---|
| חזרה לטאב | הפוסט והמקורות **נשארים** — זה התיקון עצמו |
| שליחת הודעה בצ'אט | הפוסט והמקורות **נשארים** — זה התיקון עצמו |
| סקר תקופתי כל 15 שניות במהלך מחקר | מעביר ממצאים במפורש → אין שינוי מעשי |
| הצעת שאילתות (כשנוצרת כותרת) | מעביר ממצאים במפורש → אין שינוי מעשי |
| שמירת עריכה חופשית של סיכום המחקר | מעביר ממצאים → אין שינוי מעשי |
| עריכה נקודתית של סיכום המחקר | מעביר ממצאים → אין שינוי מעשי |
| "אשר ועבור לכתיבה" | **המקורות נשארים** (היום נמחקים). הפוסט ריק ממילא בשלב הזה → אין שינוי בפוסט |
| בחירת נושא | **המקורות נשארים.** בשלב הזה אין עדיין מקורות → אין שינוי מעשי |
| שליחת חלון הגדרות הכתיבה | הפוסט הישן **נשאר על המסך** במקום להיעלם. ⚠️ זה מחייב את השינוי הנלווה — ראה למטה |
| **"שמור כסופי"** | ⚠️ **שינוי התנהגות שחורג מהתקלה הזו — דורש את החלטתך.** ראה סעיף החלטה א' |

### מה זה עלול לשבור

**1. חלון הגדרות הכתיבה — יש כאן רגרסיה אמיתית אם לא נטפל בה.**
היום, כשלוחצים "צור טיוטה ראשונה", הפוסט הישן נמחק מהמסך ובמקומו מופיעה בועת "מייצר טיוטה ב-Opus…". אחרי התיקון הפוסט הישן יישאר על המסך, והחיווי "מייצר…" **לא יופיע כלל** — כי הקוד מציג אותו רק כשאין פוסט. המשתמש ילחץ, יראה את הפוסט הישן, ולא יבין אם משהו קורה.
**המניעה:** שינוי סדר של שני תנאים בקוד הציור — אם המערכת מייצרת טיוטה, החיווי מנצח. שני שורות. זה **חלק בלתי נפרד מהתיקון**, לא שיפור נלווה.

**2. עדכון של פוסט שהגיע משרת חיצוני** — אין כזה. הפוסט מתעדכן רק דרך פעולות שהמשתמש יזם מהמסך הזה, וכולן עוברות דרך מנגנון נפרד שלא נוגעים בו.

**3. סשן שנפתח מחדש מהיסטוריה כשהוא כבר פתוח** — מזהה זהה → ייחשב כרענון, הפוסט יישאר. זו התנהגות טובה יותר מהיום (היום הוא נעלם), ולא נוצרת אי-עקביות.

**4. קריאה עם סשן ריק/שגוי** — צריך לוודא שהבדיקה דורשת מזהה אמיתי בשני הצדדים, אחרת מצב שגיאה עלול להיחשב בטעות כ"אותו סשן". פירוט בנספח.

### בדיקות

אין בדיקות קיימות בפרויקט, ולכן **אין בדיקה שנשברת**. גם אין בדיקה שמאשרת שהתיקון עובד.

### נעילה מפני באגים עתידיים

הפונקציה הזו היא נקודת החנק של כל המסך, והיא נקראת מ-14 מקומות. בלי נעילה, כל שינוי עתידי בה ישבור שוב את אותם 10 מסלולים בלי שאף אחד ישים לב.

היא גם **הדבר הכי קל בפרויקט לנעול**: היא לא נוגעת בדפדפן, לא ב-DOM, לא ברשת — היא JavaScript טהור. אפשר לנעול אותה בקובץ בדיקה אחד עם מריץ הבדיקות **המובנה של Node** — בלי להתקין שום חבילה חדשה, בלי לגעת בהגדרות הפרויקט.

**ארבע קביעות ננעלות (הנוסח המדויק בנספח):**
1. רענון של אותו סשן — הפוסט נשמר
2. רענון של אותו סשן — המקורות נשמרים
3. מעבר לסשן אחר — הפוסט **מתאפס** (זו ההגנה מפני שכפול שיורש נתונים)
4. מעבר לסשן אחר — המקורות **מתאפסים**

**זו תוספת לפרויקט, לא חלק מהתיקון — לכן היא דורשת את אישורך.** בלעדיה התיקון עדיין עובד, אבל האזור לא מוגן.

### איך נדע שזה עבד

**בדיקה חינמית לחלוטין — לא עולה כסף, לא נוגעת בנתונים:**
1. פתח סשן קיים שכבר יש בו טיוטת פוסט.
2. עבור לטאב אחר בדפדפן וחזור. → **הפוסט עדיין שם, עם כל הכפתורים.**
3. כתוב "בדיקה" בשורת ההודעות ושלח. → **הפוסט עדיין שם.**
4. הסתכל על הפאנל בצד. → **רשימת המקורות עדיין מלאה.**

**בדיקה נגדית (חשובה לא פחות):** פתח את רשימת "סשנים קודמים", עבור לסשן **אחר** שאין בו טיוטה. → **המסך נקי. אין פוסט, אין מקורות מהסשן הקודם.** אם משהו נגרר — התיקון שגוי.

### סיכון רגרסיה

**בינוני.** התיקון נוגע בנקודה שכל המסך עובר דרכה. הסיכון העיקרי אינו בתסמין המתוקן אלא ב"דליפה" בין סשנים — ולכן הבדיקה הנגדית למעלה היא הבדיקה הקריטית, לא הראשונה.

---

## תיקון 2 — שאילתה שנוספה ידנית נמחקת

**מזהה:** `A2#9` (🟠 חמור)

### מה שבור היום

קיבלת רשימת שאילתות מחקר מוצעות. לחצת "הוסף שאילתה", כתבת שאילתה משלך. לפני שלחצת "אשר והתחל מחקר" כתבת משהו בצ'אט. השאילתה שלך **נעלמה** — והמחקר היקר יצא לדרך בלי הזווית שהכי רצית.

### שורש הבעיה — שני מנגנונים, לא אחד

זה חשוב, כי תיקון של רק אחד מהם לא פותר כלום:

1. **השאילתה שהוספת מעולם לא נרשמה במאגר המרכזי.** היא נשמרה רק בעותק מקומי של רכיב התצוגה. ברגע שהצ'אט מצייר את עצמו מחדש — מכל סיבה שהיא — הרשימה נבנית מחדש מהמאגר, והתוספת שלך לא שם.
2. **וגם אם הייתה נרשמת** — הרענון הבא של הסשן היה דורס אותה בנתונים מהשרת (אותה בעיה של תיקון 1).

### התיקון

לכתוב את התוספת גם למאגר המרכזי, ולא רק לעותק המקומי; ובפונקציה המרכזית — להוסיף את רשימת השאילתות לרשימת הדברים שנשמרים ברענון של אותו סשן.

### גודל

שני מקומות: רכיב רשימת השאילתות, ושורה אחת נוספת בבלוק שנוצר בתיקון 1.

### מה עוד מגיע לקוד הזה

**רכיב השאילתות** — נקודת כניסה אחת בלבד: הוא מצויר מהצ'אט, ורק כשהסשן במצב "בהמתנה". אין לו קורא אחר, אין לו קיצור מקלדת, אין לו קישור עמוק.

**רשימת השאילתות במאגר** — ארבעה צרכנים:

| מי נוגע | האם מושפע |
|---|---|
| ציור בועת השאילתות | מקבל את הרשימה המלאה כולל התוספת ✅ |
| כפתור "אשר והתחל מחקר" | שולח לשרת את הרשימה המלאה — **כבר היום** הוא עובד מהעותק המקומי, לכן ההתנהגות שלו לא משתנה |
| בדיקת "האם כבר יש שאילתות" לפני הצעה אוטומטית | קוראת מנתוני הסשן ולא מהרשימה במאגר → לא מושפעת |
| **בחירת נושא** | ⚠️ **הנקודה היחידה שדורשת החלטה — סעיף החלטה ב'** |

### מה זה עלול לשבור

**1. הפוקוס בשדה החדש.** היום, אחרי לחיצה על "הוסף שאילתה", הסמן קופץ לשורה החדשה. אחרי התיקון, הכתיבה למאגר גורמת לצ'אט לצייר את הבועה מחדש — והשורה שהסמן היה אמור לקפוץ אליה כבר לא קיימת. בלי טיפול, **הסמן פשוט לא יופיע בשום מקום**, והמשתמש יחשוב שהכפתור לא עבד.
**המניעה:** להעביר את מיקוד הסמן לאחרי הציור מחדש. הציור קורה מיידית, ולא בהשהיה, כך שזה פתיר בשורה אחת. פירוט בנספח.

**2. עריכה של שאילתה קיימת.** דוח ה-QA קובע שעריכות של שאילתות קיימות **כן** שורדות היום. בבדיקת הקוד שלי זה נכון רק חלקית — הן שורדות את הציור המיידי, אבל אני **לא הצלחתי לוודא** שהן שורדות את הרענון המלא שמגיע מהשרת שנייה אחר כך. ראה "מה לא הצלחתי לוודא".

### בדיקות

אין בדיקות קיימות. אין בדיקה שנשברת.

### נעילה מפני באגים עתידיים

הצרכן שחייב הגנה הוא **כפתור "אשר והתחל מחקר"** — הוא מוציא כסף אמיתי, והוא הצרכן היחיד ברשימה שכשל שלו יהיה **שקט**: אם יום אחד הוא יתחיל לקרוא מהמאגר במקום מהעותק המקומי, ומשהו במאגר לא מסונכרן, המחקר ייצא לדרך עם רשימה חלקית ואף אחד לא יידע.

**בדיקה אחת לנעילה:** "שאילתה שנוספה מופיעה ברשימה שנשלחת להתחלת מחקר". גם היא JavaScript טהור וניתנת לכתיבה בלי חבילות חדשות.

### איך נדע שזה עבד

**בדיקה חינמית — אל תלחץ "אשר והתחל מחקר" בסופה:**
1. פתח סשן שנמצא במצב "בהמתנה" ויש בו רשימת שאילתות מוצעות.
2. לחץ "הוסף שאילתה". → **הסמן קופץ לשורה החדשה** (זו בדיקת הרגרסיה מסעיף 1).
3. כתוב "בדיקה 123".
4. כתוב הודעה כלשהי בצ'אט ושלח.
5. → **"בדיקה 123" עדיין ברשימה, עם הטקסט שכתבת.**
6. **אל תלחץ "התחל מחקר"** — זה יוציא כסף. סגור את הדף.

**בדיקה נגדית:** רענן את הדף (F5). → **"בדיקה 123" נעלמת.** זה **נכון וצפוי** — היא מעולם לא נשמרה בשרת. התיקון מגן מפני ציור מחדש, לא מפני רענון מלא. אם היא כן שרדה רענון מלא — מישהו הרחיב את התיקון מעבר לגבול, וזה כבר שינוי בנתונים שמורים.

### סיכון רגרסיה

**נמוך-בינוני.** האזור מבודד. הסיכון היחיד שאינו טריוויאלי הוא הפוקוס — והוא נראה לעין באופן מיידי בבדיקה.

---

## תיקון 3 — נושא מסומן והתמקדויות נגררים מסשן קודם

**מזהה:** `A2#6` (🟠 חמור)

### מה שבור היום

פתחת סשן א', סימנת כרטיס נושא, כתבת התמקדויות ("תשווה עם המצב לפני שנה") — ולא אישרת. עברת לסשן ב'. בסשן ב' יש כבר **נושא מסומן** — במיקום המקביל לזה שסימנת — ותיבת ההתמקדויות מכילה את הטקסט שכתבת בסשן א'.

אם תלחץ "אישור" בלי לשים לב, יישלח למחקר של סשן ב' נושא שלא בחרת, עם התמקדויות שלא כתבת עבורו. תוצאה: **מחקר שלם, בתשלום, על הנחיות שגויות** — בלי שום סימן חיצוני שמשהו לא בסדר.

### למה זה קורה

תיבת ההתמקדויות היא **אלמנט יחיד** ששורד בזיכרון בין סשנים. הוא לא נבנה מחדש כשעוברים סשן — הוא רק "עובר" לכרטיס במיקום המקביל. ומכיוון שמזהי הנושאים הם מספור פשוט (1, 2, 3…) שחוזר על עצמו בכל סשן, ההתאמה השגויה קורית כמעט תמיד.

### התיקון

לזכור באיזה סשן צויר האלמנט לאחרונה. אם צוירה רשימת נושאים של סשן אחר — לבנות אותו מחדש, ריק ובלי סימון.

### גודל

מקום אחד בקוד — הרכיב שמצייר את כרטיסי הנושאים. אין צורך לגעת בפונקציה המרכזית ואין צורך להוסיף קריאה חדשה משום מקום.

### מה עוד מגיע לקוד הזה

**נקודת כניסה אחת בלבד.** מיפיתי את כל הקוראים: הרכיב מצויר ממקום יחיד בצ'אט, ורק כשמתקיימים שלושה תנאים במקביל (סשן במצב "בהמתנה", בלי נושא שנבחר, ויש רשימת נושאים). אין לו קורא נוסף, אין קיצור מקלדת, אין פעולה קבוצתית, אין קישור עמוק שמגיע אליו ישירות. **זה התיקון הבטוח מבין הארבעה.**

### מה זה עלול לשבור

**התנהגות אחת שחייבת להישמר:** בתוך אותו סשן, הצ'אט מצייר את עצמו מחדש בכל מיני הזדמנויות. הקוד היום שומר בכוונה על הבחירה וההתמקדויות במקרה כזה (יש הערה מפורשת בקוד שמסבירה שזו כוונה). **התיקון חייב לאפס רק כשהמזהה של הסשן השתנה — ולא בכל ציור מחדש.** אם יאפס בכל ציור, המשתמש יאבד את מה שהקליד תוך כדי הקלדה — וזה גרוע יותר מהבאג המקורי.

### בדיקות

אין בדיקות קיימות. אין בדיקה שנשברת.

### נעילה מפני באגים עתידיים

נקודת הכניסה שאסור שתשתנה היא **"ציור מחדש בתוך אותו סשן שומר על מה שהוקלד"**. אין היום שום דבר שנועל אותה, והיא בדיוק סוג הדבר שנשבר בעתיד בלי שאף אחד ישים לב — כי היא נראית רק אם מקלידים ומחכים.

הרכיב הזה תלוי ב-DOM ולכן פחות מתאים לבדיקה אוטומטית זולה. **ההמלצה כאן היא לא בדיקה אלא הערת קוד מפורשת** ליד האיפוס: "מאפסים רק כשמזהה הסשן השתנה — איפוס בכל ציור מוחק את מה שהמשתמש מקליד". זה מתועד, זה מוצמד לשורה הנכונה, וזה לא מוסיף תשתית לפרויקט.

### איך נדע שזה עבד

**בדיקה חינמית לחלוטין — אין קריאה לשרת בשום שלב:**
1. פתח סשן במצב בחירת נושא (בחירה אוטומטית). סמן נושא, כתוב "בדיקה" בתיבת ההתמקדויות. **אל תאשר.**
2. פתח את רשימת הסשנים ועבור לסשן אחר שגם הוא בבחירת נושא.
3. → **שום כרטיס לא מסומן. תיבת ההתמקדויות לא מוצגת כלל.**

**בדיקת הרגרסיה — זו החשובה יותר:**
4. באותו סשן חדש, סמן נושא וכתוב "טקסט ארוך יותר".
5. חכה 20 שניות בלי לגעת בכלום (הצ'אט מצייר את עצמו מחדש ברקע).
6. → **הטקסט עדיין שם והכרטיס עדיין מסומן.** אם הוא נמחק — התיקון מאפס יותר מדי.

### סיכון רגרסיה

**נמוך.** קובץ אחד, נקודת כניסה אחת, אין השפעה על נתונים שמורים ואין השפעה על שום מסך אחר.

---

## תיקון 4 — חלון "צור טיוטה" נפתח מעל טיוטה קיימת

**מזהים:** `A1#7` ≡ `A4#8` (🟡 מטריד)

### מה שבור היום

אתה נכנס לסשן שכבר יש בו טיוטת פוסט. מיד קופץ חלון "איך תרצה את הפוסט?" עם הכפתור "צור טיוטה ראשונה". אם תלחץ עליו — **תיווצר טיוטה חדשה לגמרי בעלות מלאה של המודל היקר, במקום זו שכבר עבדת עליה**. סגירה ידנית ("דלג כרגע") מגלה שהטיוטה הישנה הייתה שם כל הזמן, מאחורי החלון.

### למה זה קורה

מרוץ. טעינת הטיוטה מהשרת לוקחת זמן; ההחלטה אם לפתוח את החלון מתקבלת **מיד**, לפני שהטיוטה הספיקה להגיע. באותו רגע נראה שאין טיוטה — אז החלון נפתח.

### התיקון

לדחות את ההחלטה: קודם לחכות שהטיוטה תיטען, ורק אז לבדוק אם באמת אין טיוטה. אם אין — לפתוח את החלון כרגיל.

### גודל

מקום אחד בקוד — קובץ ההפעלה של המסך. הקובץ כבר משתמש בדיוק בדפוס הזה במקום אחר (יש בו מנגנון "בקשה בדרך" לשתי פעולות אחרות), כך שהתיקון עקבי עם הקוד הקיים ולא ממציא דפוס חדש.

### מה עוד מגיע לקוד הזה

**לחלון הזה יש שתי נקודות כניסה:**

| נקודת כניסה | האם מושפעת |
|---|---|
| פתיחה אוטומטית במעבר לשלב הכתיבה | **כן — זה התיקון.** תיפתח רק אחרי שווידאנו שאין טיוטה |
| כפתור "ערוך הגדרות וכתוב מחדש" בבועת הפוסט | **לא.** הוא פותח את החלון במפורש לבקשת המשתמש, במסלול נפרד לגמרי, ולא עובר בבדיקה שאני משנה |

**וגם:** טעינת הטיוטה עצמה נקראת היום ממקום אחד בלבד (בכניסה ראשונה לסשן). התיקון יגרום לה להיקרא גם ממקום שני. חייב להיות שומר שמונע קריאה כפולה — אחרת שתי קריאות במקביל לרשימת הטיוטות. זו קריאת קריאה בלבד (לא כתיבה, לא עולה כסף), אבל היא עלולה לגרום להבהוב.

### מה זה עלול לשבור

**1. סשן שבאמת אין בו טיוטה.** אחרי התיקון החלון ייפתח **מעט מאוחר יותר** — אחרי שהשרת מאשר שאין טיוטה. עיכוב של פחות משנייה. חייבים לוודא שהחלון **בכל זאת נפתח** — אם ההמתנה תבלע את הפתיחה, המשתמש ייתקע במסך כתיבה ריק בלי שום דרך להתחיל. **זה הכישלון המסוכן של התיקון הזה, וזו הבדיקה הראשונה לבצע.**

**2. כישלון רשת בטעינת הטיוטה.** אם הקריאה נכשלת, אסור שההחלטה תיתקע. חייב להיות: נכשל → נפתח את החלון (התנהגות היום).

**3. תלות בתיקון 1.** אם תיקון 1 כבר בוצע, הטיוטה נשמרת ברענון — מה שמקטין עוד את מספר הפעמים שהחלון מנסה להיפתח לשווא. שני התיקונים מחזקים זה את זה ולא סותרים.

### בדיקות

אין בדיקות קיימות. אין בדיקה שנשברת.

### נעילה מפני באגים עתידיים

נקודת הכניסה שאסור שתשתנה: **"סשן בלי טיוטה — החלון חייב להיפתח"**. זהו כשל שקט קלאסי — אם הוא נשבר, אין הודעת שגיאה ואין קריסה, פשוט מסך ריק שאף אחד לא מבין.

הקוד הזה שזור ב-DOM ובאתחול העמוד, ולכן בדיקה אוטומטית זולה עליו לא מעשית. **ההמלצה: להוסיף שורת לוג לקונסולה** בשני המסלולים ("נמצאה טיוטה — לא פותח חלון" / "אין טיוטה — פותח חלון"). זה הופך תקלה עתידית שקטה לתקלה שנראית תוך שנייה בכלי הפיתוח, בלי להוסיף תשתית בדיקות לפרויקט.

### איך נדע שזה עבד

**שתי בדיקות, שתיהן חינמיות — ובשתיהן אל תלחץ "צור טיוטה ראשונה":**

1. **המסלול המתוקן:** פתח מ"סשנים קודמים" סשן שיש בו כבר טיוטה. → **הטיוטה מוצגת. שום חלון לא קופץ.**
2. **המסלול שחייב להמשיך לעבוד:** פתח סשן שהגיע לשלב כתיבה ואין בו עדיין טיוטה. → **החלון קופץ** (אולי בעיכוב קל). אם הוא לא קופץ — התיקון שבור, והמשתמש נתקע.

### סיכון רגרסיה

**נמוך** לביצוע, **אבל הכישלון האפשרי חמור** (משתמש תקוע במסך ריק). לכן בדיקה 2 היא חובה, לא אופציה.

---

## ❓ דורש החלטה

### א. `A4#5` — "שמור כסופי" מפסיק לנעול את כפתורי העריכה

**התקלה:** היום, אחרי שלוחצים "שמור כסופי", כפתורי העריכה מפסיקים להגיב ומונה המילים מציג 0. זה ממצא 🟠 נפרד במניפסט (`A4#5`), **מחוץ לקבוצה שלי**.

**מה יקרה בלי שביקשתי:** תיקון 1 מסיר את התסמין הזה **כתופעת לוואי**. אותו איפוס שמוחק את הפוסט במעבר טאב הוא בדיוק זה שמנתק את הכפתורים אחרי "שמור כסופי". ברגע שמפסיקים לאפס — הכפתורים ממשיכים לעבוד והמונה ממשיך להציג את המספר הנכון.

**למה זו לא סתם בשורה טובה:** יש שאלת מוצר פתוחה במניפסט — `A4#18`: **"שמור כסופי" צריך לנעול באמת?** אם התשובה שלך היא "כן, נעילה זו נעילה" — אז המצב שנוצר אחרי תיקון 1 (כפתורים פעילים אחרי נעילה) הוא **פחות** נכון מהיום, גם אם הוא נראה טוב יותר. הנעילה הנוכחית מקרית וחלקית, אבל היא קיימת.

**האפשרויות:**
- **לקבל** — הכפתורים ימשיכו לעבוד אחרי "שמור כסופי". פשוט, ותואם את `A4#11` (עריכה אחרי נעילה לא מתעדכנת ממילא — כלומר הנעילה כבר לא באמת עובדת).
- **לחסום במפורש** — להשאיר את האיפוס רק במסלול של "שמור כסופי". זה מוסיף חריגה נקודתית לפונקציה המרכזית: יותר קוד, יותר סיכון, ומעשית הוא משמר באג.
- **לדחות** — לתקן את תיקון 1 עכשיו, ולהחליט על משמעות הנעילה בנפרד יחד עם `A4#18` ו-`A4#11`.

**המלצתי:** לקבל, ולפתוח את שאלת הנעילה בנפרד. **אבל זו שאלת מוצר ולא הכרעה טכנית — אני לא מכריע אותה.**

**מה קורה אם לא נעשה כלום:** תיקון 1 לא יכול להתבצע בלי לענות על השאלה. זה חוסם 🔴 חוסם.

---

### ב. `A2#9` — האם רשימת השאילתות במסך צריכה לשרוד רענון של אותו סשן

**התקלה:** שאילתה שהוספת ידנית נמחקת.

**מה אני רוצה לשנות:** שרשימת השאילתות שבמסך לא תידרס בכל רענון של אותו סשן.

**למה אי אפשר בלי זה:** לתקן רק את הכתיבה למאגר לא מספיק — הרענון הבא מהשרת ידרוס אותה שוב. בדקתי: זו לא השערה, זה מסלול ודאי.

**על מה זה ישפיע — נקודה אחת שדורשת את הכרעתך:**
כשמשתמש **בוחר נושא**, השרת **מוחק בכוונה** את רשימת השאילתות (כדי שייבנו מחדש לנושא החדש). אם המסך ישמור על הרשימה שלו ברענון, הוא לא יראה את המחיקה הזו.

**האם זה מתממש בפועל?** בדקתי — כמעט בוודאות לא: בשלב בחירת הנושא עדיין אין שאילתות במסך, ולפי ממצא `A2#4` **אין בכלל דרך בממשק לחזור ולבחור נושא אחר אחרי שכבר נבחר**. כלומר, המסלול שבו זה מזיק חסום היום ממילא.

**האפשרויות:**
- **לשמור תמיד** (המלצתי) — פשוט, מכסה את התקלה, מסתמך על כך שהמסלול המסוכן חסום.
- **לשמור חוץ מבבחירת נושא** — חסין לחלוטין, אבל מוסיף חריגה נקודתית לפונקציה המרכזית, ומצמיד את הפונקציה למסלול ספציפי אחד. יותר קוד, פחות ברור.

**מה קורה אם לא נעשה כלום:** `A2#9` נשאר. מחקר בתשלום ייצא לדרך בלי הזווית שהמשתמש הכי רצה, בלי סיכוי סביר שהוא ישים לב.

---

### ג. `X2` — תיבת הצ'אט שלא עושה כלום

**התקלה:** כתיבת הודעה בתיבת הצ'אט לא מייצרת שום תגובה מהבוט. **זו החלטה מכוונת ומתועדת בקוד** — במניפסט היא מסומנת 🎯, ולכן **אני לא מתקן אותה ולא מציע לתקן**.

**מה משתנה אחרי התיקונים כאן:** היום, כל שליחה כזו מוחקת מהמסך שלושה דברים — פוסט, מקורות, ושאילתה שהוספת. אחרי תיקונים 1 ו-2, **כל הנזק הזה נעלם**. נשאר רק שהפעולה לא עושה כלום.

**מה נשאר לא פתור:** משתמש שיכתוב "תקצר קצת" עדיין לא יקבל תשובה ולא יבין למה. זה כבר לא הרסני — אבל זה עדיין מבלבל.

**מה אני רוצה לשנות:** לכתוב שורת הסבר בתיבה, או לנטרל אותה בשלבים שבהם אין לה תפקיד.

**למה אני לא מחליט לבד:** זה שינוי בטקסט ובממשק שהמשתמש רואה, מחוץ לגבול התקלה, וזה נוגד את הכלל בהוראות הפרויקט ("לעולם לא לשנות עיצוב וממשק בלי בקשה מפורשת").

**האפשרויות:**
- **לא לעשות כלום** — לגיטימי לגמרי אחרי תיקונים 1–2, כי הנזק כבר לא קיים.
- **לשנות את טקסט העזר בתיבה** — למשל "הודעות נשמרות ביומן הסשן. הבוט משיב דרך הבועות ולא כאן." שינוי מילים אחד, בלי נגיעה בעיצוב או בפריסה.
- **לנטרל את התיבה בשלבים מסוימים** — הכי ברור, אבל מוריד יכולת קיימת (שמירת הערות ביומן הסשן), וזה כבר שינוי מוצר של ממש.

**מה קורה אם לא נעשה כלום:** משתמש יבזבז זמן על ניסיונות לדבר עם הבוט. **לא מסוכן יותר — רק מבלבל.**

---

## ממתין להחלטתך — לא נכתבה תוכנית

אין בקבוצה שלי אף ממצא שסומן ❓ במניפסט. שלושת סעיפי ההחלטה למעלה הם **שאלות שנולדו מהתכנון עצמו**, לא ממצאי QA פתוחים.

**לידיעה:** שלוש שאלות מוצר במניפסט משיקות לאזור הזה ומשפיעות עליו — `A4#18` ("שמור כסופי" צריך לנעול באמת?), `A4#11` (עריכה אחרי נעילה), ו-`A2#4` (אין דרך לחזור ולבחור נושא אחר). שתי הראשונות קשורות לסעיף החלטה א', השלישית לסעיף ב'. **הן לא בגבולות התכנית הזו ולא תוקנו כאן.**

---

## מה לא הצלחתי לוודא

כל אלה נבדקו בקוד בלבד. **לא הרצתי את המערכת, לא התחברתי, ולא נגעתי בבסיס הנתונים** — בגלל האילוץ שהלוקאלי והפרודקשן חולקים DB.

1. **עריכה של שאילתה קיימת אחרי רענון מלא.** דוח ה-QA קובע שעריכות שורדות והוספות לא. בקריאת הקוד, העריכות שורדות את הציור המיידי — אבל **לא הצלחתי לקבוע בוודאות** אם הן שורדות את הרענון מהשרת שמגיע שנייה אחר כך. אם לא — יש כאן תקלה נוספת שקטה שהתכנית הזו לא מכסה.
   **איך לבדוק בדקה:** פתח סשן עם שאילתות, ערוך טקסט של שאילתה קיימת, שלח הודעה בצ'אט, חכה שלוש שניות. אם העריכה שרדה — הדוח צודק והתכנית שלמה. אם לא — עדכן אותי, זה מרחיב את תיקון 2.

2. **מספר הציורים מחדש בפועל.** התכנית מניחה שכתיבה למאגר גורמת לצ'אט להצטייר מחדש באופן מיידי וסינכרוני. זה **ברור מהקוד**, אבל לא נמדד בדפדפן. אם יש עיכוב שלא ראיתי, הטיפול בפוקוס בתיקון 2 עשוי לדרוש התאמה קטנה.

3. **מובייל.** אף אחד מארבעת התיקונים לא נוגע בעיצוב, בפריסה או ב-CSS — ולכן אין סיבה תיאורטית להשפעה על מובייל. **לא בדקתי בפועל בשום רוחב מסך.**

4. **התנהגות עם שני טאבים פתוחים על אותו סשן.** אחרי תיקון 1, טאב שמחזיק פוסט לא יעודכן מפוסט שנוצר בטאב אחר, עד רענון. **היום ההתנהגות שונה** (איפוס בכל רענון) — אבל היום היא גם שבורה, ובפועל היא מוחקת ולא מסנכרנת. לא הצלחתי לוודא האם מישהו עובד בשני טאבים בפועל.

5. **ערך פנימי מת.** מצאתי בקוד שדה שנקרא "מזהה טיוטת הסיכום" שנכתב אבל **אף אחד לא קורא אותו**. הוא לא קשור לאף ממצא בקבוצה, לא מזיק, ולא נגעתי בו — מציין רק כדי שלא ייראה כאילו התעלמתי.

---

## נספח טכני

> החלק הזה מיועד למי שיבצע את התיקון. הכתובות מדויקות למצב הקוד ב-26.07.2026.

### מפת נקודות הכניסה ל-`Store.setSession()`

**קובץ:** `admin/public/js/whatsapp-updates/state.js:40-63`

| # | קורא | `extras.findings`? | סוג | מושפע מתיקון 1? |
|---|---|---|---|---|
| 1 | `index.js:159` — polling כל 15 שניות | ✅ מועבר | אותו סשן | לא מעשית |
| 2 | `index.js:187` — אחרי `proposeQueries` עם title | ✅ מועבר | אותו סשן | לא מעשית |
| 3 | `index.js:318` — `visibilitychange` | ✅ מועבר | אותו סשן | **כן — התיקון** |
| 4 | `index.js:331` — resume מ-`?session=` | ✅ מועבר | **מעבר סשן** | לא |
| 5 | `composer.js:40` — אחרי שליחת הודעה | ❌ חסר | אותו סשן | **כן — התיקון** |
| 6 | `chat-view.js:137` — שמירת עריכה חופשית של הסיכום | ✅ מועבר | אותו סשן | לא מעשית |
| 7 | `chat-view.js:295` — `acceptResearch` | ❌ חסר | אותו סשן | **כן — findings** |
| 8 | `sessions-list.js:57` — פתיחה מהיסטוריה | ✅ מועבר | **מעבר סשן** | לא |
| 9 | `sessions-list.js:75` — אחרי `duplicate` | ❌ חסר | **מעבר סשן** | לא (וזה קריטי) |
| 10 | `onboarding-modal.js:78` — סשן חדש | ❌ חסר | **מעבר סשן** | לא (וזה קריטי) |
| 11 | `selection-popover.js:149` — עריכה נקודתית בסיכום | ✅ מועבר | אותו סשן | לא מעשית |
| 12 | `topic-discovery.js:157` — `selectTopic` | ❌ חסר | אותו סשן | **כן — findings + queries** |
| 13 | `writing-config-modal.js:152` — `patchSession` | ❌ חסר | אותו סשן | **כן — post** ⚠️ |
| 14 | `writing-view.js:272` — אחרי `finalize` | ✅ מועבר | אותו סשן | **כן — post** ⚠️ סעיף החלטה א' |

### תיקון 1 — `state.js:40-63`

השינוי המוצע ל-`setSession`:

```js
setSession(session, extras = {}) {
  // Two callers with two different intents share this function:
  //   • switching to a DIFFERENT session  → reset everything (4 call sites)
  //   • refreshing the CURRENT session    → keep screen-only state (10 call sites)
  // Both ids must be truthy — an undefined session must NEVER count as "same".
  const sameSession = !!session?.id && session.id === data.session?.id;

  data.session  = session;
  data.messages = Array.isArray(session?.messages) ? session.messages : [];
  data.topics   = Array.isArray(session?.discovered_topics) ? session.discovered_topics : [];

  // ── queries: תיקון 2 מוסיף כאן שורה, ראה למטה ──
  data.queries  = Array.isArray(session?.proposed_queries) ? session.proposed_queries : [];

  // findings live only in memory during a run — the server payload carries them
  // only when the caller asked for them (8 of 14 call sites do).
  data.findings = Array.isArray(extras.findings)
    ? extras.findings
    : (sameSession ? data.findings : []);

  data.research = {
    running: session?.status === 'researching',
    tokens: Number(session?.tokens_used || 0),
    cost: Number(session?.estimated_cost_usd || 0),
    summary: session?.research_summary || null,
    summaryDraftId: null
  };

  // The post draft is NOT part of the session row — it lives in whatsapp_drafts
  // and is fetched separately by loadLatestPostDraft(). Rebuilding it from
  // session.final_post on a same-session refresh wipes it (A4#1 / A1#3).
  data.post = sameSession ? data.post : {
    draftId: null,
    content: session?.final_post || '',
    lengthPref: session?.length_pref || 'medium',
    wordCount: 0,
    generating: false
  };

  data.error = null;
  emit();
}
```

**שינוי נלווה חובה — `chat-view.js:305-312`.** היום סדר התנאים הוא:

```js
if ((status === 'writing' || status === 'done') && state.post.content) {
  renderWritingBubble(listEl);
} else if (state.post.generating) {
  // typing indicator
}
```

מכיוון ש-`post.content` כבר לא מתאפס ב-`writing-config-modal.js:152`, הענף הראשון תמיד ינצח והחיווי "מייצר טיוטה ב-Opus…" לא יופיע לעולם. **יש להפוך את סדר התנאים** כך ש-`state.post.generating` נבדק ראשון. אין שינוי CSS ואין שינוי מחלקות — רק סדר.

### תיקון 2 — `query-list.js` + שורה אחת ב-`state.js`

**א. `state.js`** — בתוך הבלוק שנוצר בתיקון 1:

```js
data.queries = Array.isArray(extras.queries)
  ? extras.queries
  : (sameSession && data.queries.length > 0
      ? data.queries
      : (Array.isArray(session?.proposed_queries) ? session.proposed_queries : []));
```

> הערה: `extras.queries` אינו בשימוש כיום באף קורא. הוא נוסף רק כדי לאפשר דריסה מפורשת בעתיד בלי לגעת שוב בפונקציה. אם מעדיפים מינימליות מוחלטת — אפשר להשמיט אותו ולהשאיר רק את בדיקת `sameSession`.

**ב. `query-list.js:112-118`** — הכפתור "הוסף שאילתה". היום:

```js
addBtn.addEventListener('click', () => {
  queriesRef.push({ id: uid(), text: '', lang: 'en', rationale: '', enabled: true });
  repaint();
  const lastRow = list.lastElementChild;
  lastRow?.querySelector('.wa-query-text')?.focus();
});
```

הבעיה: `queriesRef` הוא עותק מקומי (`queries.slice()` בשורה 70). הדחיפה לעולם לא מגיעה ל-Store, ולכן הציור מחדש הבא של הצ'אט (`chat-view.js:277`) בונה את הבועה מ-`state.queries` בלי השורה החדשה.

התיקון:

```js
addBtn.addEventListener('click', () => {
  queriesRef.push({ id: uid(), text: '', lang: 'en', rationale: '', enabled: true });
  // Write through to the Store — Store.subscribe → chat-view re-render is
  // SYNCHRONOUS (state.js:30-34), so by the time this returns the bubble has
  // already been rebuilt and `list` below is detached. Query the LIVE DOM.
  Store.setQueries(queriesRef.slice());
  document.querySelector('.wa-queries-list')?.lastElementChild
    ?.querySelector('.wa-query-text')?.focus();
});
```

**קריטי:** `emit()` ב-`state.js:30` סינכרוני, ו-`chat-view.js:256-258` מוחק את `listEl.innerHTML` ובונה בועה חדשה. המשתנה `list` בקלוז'ר מצביע אז לאלמנט מנותק. `repaint()` המקורי מיותר (הבועה נבנית מחדש ממילא), ומיקוד על `list.lastElementChild` פשוט לא ייראה. **זו הסיבה שהפוקוס חייב לעבור דרך ה-DOM החי.**

**ג. אופציונלי — `query-list.js:89` `commit()`.** אם מחליטים לכסות גם עריכות (ראה "מה לא הצלחתי לוודא" #1): `function commit() { Store.setQueries(queriesRef.slice()); }`. זה בטוח — ה-snapshot ב-`chat-view.js:213` משווה `queryCount` בלבד, ועריכת טקסט לא משנה את המונה, ולכן `unchanged` יחזיר `true` והצ'אט לא יצייר מחדש. **לא לבצע לפני שהבדיקה בסעיף "מה לא הצלחתי לוודא" #1 מאשרת שיש בעיה.**

### תיקון 3 — `topic-discovery.js`

המשתנים ברמת המודול, `topic-discovery.js:24-25`:

```js
let expansionEl = null;
let confirmHandler = null;
```

הם שורדים מעבר סשנים. `getSelectedTopicId()` (`:50-53`) קורא `expansionEl.parentElement.dataset.topicId` — וגם אחרי ש-`chat-view.js:258` מבצע `listEl.innerHTML = ''`, ה-`parentElement` עדיין מצביע לכרטיס המנותק, שעדיין מחזיק `dataset.topicId`. מכיוון שמזהי הנושאים הם `'1'`, `'2'`, `'3'`… בכל סשן — `cards.has(selectedId)` בשורה 185 מצליח, ו-`moveExpansionTo` מחזיר את התיבה עם הטקסט הישן.

התיקון, בראש `renderDiscoveryBubble` (`:106`), אחרי `if (!session) return;`:

```js
// Reset the shared expansion element when the session changes. Topic ids are
// per-session counters ('1','2','3'), so a stale selection matches a card in
// the NEW session almost every time (A2#6).
// Reset ONLY on session change — resetting on every repaint would wipe what the
// user is typing mid-keystroke (chat-view re-renders on background ticks).
if (lastDiscoverySessionId !== session.id) {
  lastDiscoverySessionId = session.id;
  expansionEl = null;
  confirmHandler = null;
}
```

בתוספת `let lastDiscoverySessionId = null;` ליד שאר משתני המודול.

הערת הקוד הזו היא ה"נעילה" שהומלצה בגוף התכנית — אין תשתית בדיקות שמצדיקה בדיקה אוטומטית לרכיב תלוי-DOM.

### תיקון 4 — `index.js`

הבעיה: `index.js:377-383` מוערך סינכרונית בתוך `Store.subscribe`, בעוד `loadLatestPostDraft()` (`:387-405`, נקרא מ-`:355`) אסינכרוני.

התיקון המוצע — להוסיף שומר ל-`inflight` (`:136`) ולעטוף את ההחלטה:

```js
// index.js:136
const inflight = { proposeQueries: false, discoverTopics: false, loadDraft: false };

// index.js:387 — הוספת שומר ל-loadLatestPostDraft
async function loadLatestPostDraft() {
  if (inflight.loadDraft) return;
  const s = Store.get().session;
  if (!s) return;
  if (!['writing', 'done'].includes(s.status)) return;
  inflight.loadDraft = true;
  try {
    // ... הגוף הקיים ללא שינוי ...
  } catch (err) {
    console.warn('failed to load post drafts', err);
  } finally {
    inflight.loadDraft = false;
  }
}

// פונקציה חדשה
async function openWritingConfigIfNoDraft(session, writingConfig) {
  await loadLatestPostDraft();          // no-op אם כבר רץ או כבר נטען
  const st = Store.get();
  if (st.session?.id !== session.id) return;   // המשתמש עבר סשן בינתיים
  if (st.post.content || st.post.generating) {
    console.debug('[wa] draft found — writing-config modal not opened');
    return;
  }
  console.debug('[wa] no draft — opening writing-config modal');
  writingConfig.open(session);
}
```

ובקריאה עצמה (`index.js:377-383`):

```js
if (status !== lastStatus) {
  lastStatus = status;
  if (status === 'writing' && !state.post.content && !state.post.generating) {
    openWritingConfigIfNoDraft(state.session, writingConfig);
  }
}
```

**שלוש נקודות שחייבות לעבור בדיקה בקוד לפני מסירה:**
1. `loadLatestPostDraft()` **חייב** להיות מסוגל להיקרא פעמיים ברצף — פעם מ-`:355` ופעם מכאן. השומר `inflight.loadDraft` מונע קריאה כפולה, אבל **שים לב:** אם הקריאה מ-`:355` כבר רצה, ה-`await` כאן יחזור מיד (השומר מחזיר `undefined`) **לפני** שהטיוטה נטענה — והמודל ייפתח בכל זאת. הפתרון: לשמור את ה-Promise עצמו ב-`inflight.loadDraft` במקום דגל בוליאני, ולעשות לו `await`.
   **זו הנקודה היחידה בכל התכנית שקל לטעות בה, והיא בדיוק זו שגורמת לבאג המקורי.**
2. במסלול `catch` — `openWritingConfigIfNoDraft` חייב להמשיך ולפתוח את המודל.
3. הבדיקה `st.session?.id !== session.id` מונעת פתיחת מודל על סשן שהמשתמש כבר עזב.

### נעילה — קובץ בדיקה מוצע (טעון אישור)

מריץ הבדיקות המובנה של Node (`node --test`) — **בלי חבילות חדשות, בלי שינוי ב-`package.json`**:

```
admin/public/js/whatsapp-updates/state.test.js
```

ארבע הקביעות (תיקון 1) + אחת (תיקון 2):

| # | תרחיש | הקביעה |
|---|---|---|
| 1 | `setSession(A)` → `setPost({content:'x'})` → `setSession(A)` | `post.content === 'x'` |
| 2 | `setSession(A, {findings:[f]})` → `setSession(A)` | `findings.length === 1` |
| 3 | `setSession(A)` → `setPost({content:'x'})` → `setSession(B)` | `post.content === ''` |
| 4 | `setSession(A, {findings:[f]})` → `setSession(B)` | `findings.length === 0` |
| 5 | `setSession(A)` → `setQueries([q1,q2])` → `setSession(A)` | `queries.length === 2` |

3 ו-4 הן החשובות — הן נועלות את ההגנה מפני דליפה בין סשנים (השכפול שיורש נתונים). המאגר הוא JavaScript טהור בלי תלות ב-DOM או ברשת, ולכן הקובץ הזה ירוץ כמו שהוא.

### לתשומת לב המבצע

- כל ארבעת התיקונים בתיקיית `admin/`. לפי `CLAUDE.md`, בסיום העבודה יש לכתוב "עדכנתי גם את גרפיפיי".
- אין שינוי CSS, עיצוב, צבעים או פריסה באף אחד מהתיקונים. השינוי היחיד ב-`chat-view.js` הוא **סדר תנאים**, לא מחלקות ולא סגנון.
- אין שינוי בקוד השרת ואין שינוי בסכימת הנתונים. אין צורך ב-migration ואין צורך ב-backfill.
